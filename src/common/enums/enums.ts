export enum TaskStatuses {
    New,
    InProgress,
    Completed,
    Draft,
}

export enum TaskPriorities {
    Low ,
    Middle,
    Hi,
    Ungently,
    Later,
}

export enum ResultCode {
    Success ,
    Error ,
    Captcha = 10,
}